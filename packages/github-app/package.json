{
  "name": "@mcp-scanner/github-app",
  "version": "1.0.0",
  "description": "GitHub App that scans PRs for insecure MCP config files",
  "scripts": {
    "build": "tsc",
    "start": "probot run ./dist/index.js",
    "dev": "nodemon --exec 'npm run build && npm start'"
  },
  "dependencies": {
    "probot": "^13.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
